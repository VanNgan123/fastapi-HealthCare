services:
  - type: web
    name: fastapi-healthcare-chatbot
    env: python
    pythonVersion: 3.11.7
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"